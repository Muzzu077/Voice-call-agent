# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# LLM
ollama>=0.4.0

# Database (PostgreSQL)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
alembic>=1.13.0

# Legacy SQLite (kept for migration/fallback)
aiosqlite>=0.20.0

# Auth
bcrypt>=4.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4
email-validator>=2.0

# Memory / Vectors
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Audio (Phase 2)
silero-vad-lite>=0.2.0
faster-whisper>=1.0.0
sounddevice>=0.5.0
numpy>=1.26.0

# TTS (Phase 3+)
# kokoro>=0.9.0

# Scheduling
apscheduler>=3.10.0

# Telephony (Phase 4+)
twilio>=9.0.0

# Utilities
httpx>=0.27.0

# Desktop Automation (runs on host, NOT in Docker)
pyautogui>=0.9.54
pyperclip>=1.8.0
